name: Frontend Deploy

on:
  workflow_dispatch:
    inputs:
      version:
        required: true
      environment:
        required: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy frontend
        run: |
          echo "Deploying frontend ${{ github.event.inputs.version }} to ${{ github.event.inputs.environment }}"
